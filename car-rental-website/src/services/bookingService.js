const bookingService = {
    // Các method giả
    getUserBookings: async () => ({ success: true, data: [] }),
    getBookingDetails: async () => ({ success: true, data: {} }),
    // Thêm các method khác nếu cần
  };
  
  export default bookingService;
